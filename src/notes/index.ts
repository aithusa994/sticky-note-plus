import { createStickyNote, loadStickyNotes, clearAllNotes } from "./notes";
import { saveNote, removeNote, exportNotes, getAllNotes } from "./storage";

export {
  saveNote,
  removeNote,
  exportNotes,
  getAllNotes,
  createStickyNote,
  loadStickyNotes,
  clearAllNotes,
};
